{
  "name": "poclanding",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "esbuild public/Orbitron.json --bundle --outfile=dist/Orbitron.json --format=esm && esbuild src/slider-loop.js --bundle --outfile=dist/slider-loop.js --format=esm && esbuild src/shaderScroll.js --bundle --outfile=dist/shaderScroll.js --format=esm && esbuild src/shaders.js --bundle --outfile=dist/shaders.js --format=esm && esbuild src/portfolio.js --bundle --outfile=dist/portfolio.js --format=esm && mkdir -p dist/assets && cp public/portfolio.glb dist/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "vite": "^6.0.1"
  },
  "dependencies": {
    "dat.gui": "^0.7.9",
    "dotenv": "^16.4.7",
    "gsap": "^3.12.5",
    "three": "^0.171.0"
  }
}
